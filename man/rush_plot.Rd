% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{rush_plot}
\alias{rush_plot}
\title{Create a quick plot}
\usage{
rush_plot(
  file = "-",
  x = NULL,
  y = NULL,
  z = NULL,
  color = NULL,
  fill = NULL,
  alpha = NULL,
  size = NULL,
  shape = NULL,
  group = NULL,
  geom = "auto",
  facets = NULL,
  log = NULL,
  title = NULL,
  xlab = NULL,
  ylab = NULL,
  margins = FALSE,
  pre = NULL,
  post = NULL,
  output = NULL,
  output_format = "auto",
  width = NULL,
  height = NULL,
  units = "in",
  dpi = 300,
  input_format = "auto",
  delimiter = ",",
  input_delimiter = NULL,
  header = TRUE,
  input_header = NULL,
  names = NULL,
  clean_names = TRUE,
  library = NULL,
  tidyverse = FALSE,
  input_sheet = NULL,
  seed = NULL,
  dry_run = FALSE,
  no_ir = FALSE,
  no_rush = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{file}{Character vector of input file paths. The reader is chosen by
extension (override with \code{input_format}). Use \code{"-"} to read from standard
input. The first file is available as \code{df}.}

\item{x, y, z}{Column names for the x, y, and z aesthetics (as strings).}

\item{color, fill, alpha, size, shape, group}{Column names for additional
aesthetics (as strings).}

\item{geom}{Geometry to use. One of \code{"auto"}, \code{"point"}, \code{"line"},
\code{"histogram"}, \code{"bar"}, \code{"boxplot"}, \code{"density"}, etc. When \code{"auto"},
\code{geom_point()} is used if both \code{x} and \code{y} are set, \code{geom_histogram()}
otherwise.}

\item{facets}{A facet specification as a string. Use a two-sided formula
(e.g. \code{"gear ~ cyl"}) for \code{\link[ggplot2:facet_grid]{ggplot2::facet_grid()}} or a one-sided formula
(e.g. \code{"~ cyl"}) for \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.}

\item{log}{Which axes to log-transform. One of \code{"x"}, \code{"y"}, or \code{"xy"}.}

\item{title}{Plot title.}

\item{xlab, ylab}{Axis labels.}

\item{margins}{If \code{TRUE}, display marginal facets (only with
\code{\link[ggplot2:facet_grid]{ggplot2::facet_grid()}}).}

\item{pre}{A character string of R code to run before plotting (e.g. to
transform \code{df}).}

\item{post}{A character string of R code to run after plotting (e.g. to
add layers to \code{p}).}

\item{output}{Path to save the plot. When \code{NULL} (the default), renders to
the terminal as ANSI art if interactive, or streams PNG to stdout.}

\item{output_format}{Output format override. Use \code{"ansi"} for colored
terminal art or \code{"ascii"} for plain-text characters without escape codes.
Both force terminal rendering regardless of TTY detection. When \code{"auto"}
(the default), the format is inferred from \code{output} or the terminal.}

\item{width, height}{Plot dimensions. In terminal mode, \code{width} is in
characters; for file output, in \code{units}.}

\item{units}{Size units for file output. One of \code{"in"}, \code{"cm"}, \code{"mm"}, or
\code{"px"}.}

\item{dpi}{Resolution for file output.}

\item{input_format}{Input format override. When \code{"auto"} (the default), the
format is inferred from the file extension.}

\item{delimiter}{Input column delimiter. Defaults to \code{","}.}

\item{input_delimiter}{Input delimiter. Overrides \code{delimiter} for reading.}

\item{header}{If \code{FALSE}, the input file is read without column names.}

\item{input_header}{If \code{FALSE}, the input file is read without column
names. Overrides \code{header} for reading.}

\item{names}{Comma-separated column names to use (e.g. \code{"a,b,c"}).
Implies \code{input_header = FALSE}.}

\item{clean_names}{If \code{TRUE} (the default), column names are cleaned with
\code{\link[janitor:clean_names]{janitor::clean_names()}}.}

\item{library}{Character vector of package names to load in the generated
script.}

\item{tidyverse}{If \code{TRUE}, loads the tidyverse and glue packages.}

\item{input_sheet}{Sheet to read from an Excel file. Either a string (sheet
name) or an integer (sheet index).}

\item{seed}{Integer seed for the random number generator.}

\item{dry_run}{If \code{TRUE}, print the generated script instead of executing
it.}

\item{no_ir}{If \code{TRUE}, execute the script with \code{Rscript} instead of \code{ir}.}

\item{no_rush}{If \code{TRUE}, generate fully inlined code with no dependency on
the rush package.}

\item{verbose}{If \code{TRUE}, print debugging information to stderr.}
}
\value{
Invisibly returns the exit status of the script (integer), or
\code{NULL} when \code{dry_run = TRUE}.
}
\description{
Generates a self-contained R script that reads data and produces a ggplot2
visualization, then executes it with \href{https://r-lib.github.io/ir/}{ir}.
When writing to a terminal, the plot is rendered as ANSI art; otherwise it
is saved to a file or streamed as PNG.
}
\examples{
# Preview a scatter plot script
rush_plot("mtcars.csv", x = "wt", y = "mpg", dry_run = TRUE)

# Histogram with color
rush_plot("mtcars.csv", x = "mpg", color = "cyl",
          geom = "histogram", dry_run = TRUE)

# Faceted plot with title
rush_plot("mtcars.csv", x = "wt", y = "mpg",
          facets = "~ cyl", title = "Weight vs MPG", dry_run = TRUE)

\dontrun{
# Actually execute (requires ir on PATH)
rush_plot("mtcars.csv", x = "wt", y = "mpg")
rush_plot("mtcars.csv", x = "wt", y = "mpg",
          output = "plot.png", width = 8, height = 6)
}

}
\seealso{
\code{\link[=rush_run]{rush_run()}}, \code{\link[=rush_sql]{rush_sql()}}, \code{\link[=rush_convert]{rush_convert()}}
}
