% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert.R
\name{rush_convert}
\alias{rush_convert}
\title{Convert between file formats}
\usage{
rush_convert(
  file,
  output = NULL,
  output_format = "auto",
  input_format = "auto",
  delimiter = ",",
  input_delimiter = NULL,
  output_delimiter = NULL,
  header = TRUE,
  input_header = NULL,
  output_header = NULL,
  names = NULL,
  clean_names = TRUE,
  head = NULL,
  input_sheet = NULL,
  output_root = "root",
  output_record = "record",
  output_sheet = NULL,
  output_indent = 2L,
  dry_run = FALSE,
  no_ir = FALSE,
  no_rush = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{file}{Character vector of input file paths. At least one file is
required. The reader is chosen by extension (override with
\code{input_format}). Use \code{"-"} to read from standard input.}

\item{output}{Output file path. The format is inferred from the extension
unless \code{output_format} is set. Supports template placeholders for
multi-file conversion: \verb{\%(file_name)s} and \verb{\%(file_index)d}.}

\item{output_format}{Output format override. When \code{"auto"} (the default),
the format is inferred from \code{output}.}

\item{input_format}{Input format override. When \code{"auto"} (the default), the
format is inferred from the file extension.}

\item{delimiter}{Character used as both input and output column delimiter.
Defaults to \code{","}. Overridden by \code{input_delimiter} or \code{output_delimiter}
when set.}

\item{input_delimiter}{Input delimiter. Overrides \code{delimiter} for reading.}

\item{output_delimiter}{Output delimiter. Overrides \code{delimiter} for writing.}

\item{header}{If \code{FALSE}, suppress headers on both input and output.
Overridden by \code{input_header} or \code{output_header} when set.}

\item{input_header}{If \code{FALSE}, the input file is read without column
names. Overrides \code{header} for reading.}

\item{output_header}{If \code{FALSE}, omit the header row when writing
delimited output. Overrides \code{header} for writing.}

\item{names}{Comma-separated column names to use (e.g. \code{"a,b,c"}).
Implies \code{input_header = FALSE}.}

\item{clean_names}{If \code{TRUE} (the default), column names are cleaned with
\code{\link[janitor:clean_names]{janitor::clean_names()}}.}

\item{head}{Integer. Limit the output to this many rows.}

\item{input_sheet}{Sheet to read from an Excel file. Either a string (sheet
name) or an integer (sheet index).}

\item{output_root}{Root element name for XML output.}

\item{output_record}{Record element name for XML/TOML output.}

\item{output_sheet}{Excel sheet name for xlsx output.}

\item{output_indent}{Indentation level for JSON/YAML output.}

\item{dry_run}{If \code{TRUE}, print the generated script instead of executing
it.}

\item{no_ir}{If \code{TRUE}, execute the script with \code{Rscript} instead of \code{ir}.}

\item{no_rush}{If \code{TRUE}, generate fully inlined code with no dependency on
the rush package.}

\item{verbose}{If \code{TRUE}, print debugging information to stderr.}
}
\value{
Invisibly returns the exit status of the script (integer), or
\code{NULL} when \code{dry_run = TRUE}.
}
\description{
Generates a self-contained R script that reads one or more input files and
writes them in a different format, then executes it with
\href{https://r-lib.github.io/ir/}{ir}.
}
\examples{
# Preview a CSV-to-Parquet conversion
rush_convert("data.csv", output = "data.parquet", dry_run = TRUE)

# Multiple files with a template
rush_convert(c("a.csv", "b.csv"),
             output = "\%(file_name)s.parquet", dry_run = TRUE)

\dontrun{
# Actually execute (requires ir on PATH)
rush_convert("data.csv", output = "data.parquet")
rush_convert("data.json", output = "data.csv")
}

}
\seealso{
\code{\link[=rush_run]{rush_run()}}, \code{\link[=rush_sql]{rush_sql()}}, \code{\link[=rush_plot]{rush_plot()}}
}
