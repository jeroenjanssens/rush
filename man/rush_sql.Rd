% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sql.R
\name{rush_sql}
\alias{rush_sql}
\title{Query files with SQL}
\usage{
rush_sql(
  query = NULL,
  file = character(),
  output = NULL,
  output_format = "auto",
  delimiter = ",",
  output_delimiter = NULL,
  output_header = NULL,
  library = NULL,
  tidyverse = FALSE,
  head = NULL,
  seed = NULL,
  dry_run = FALSE,
  no_ir = FALSE,
  no_rush = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{query}{A character string containing a DuckDB SQL query. Required.}

\item{file}{Character vector of file paths to expose to the query. Each file
becomes a DuckDB relation named after its base name (lowercased,
non-alphanumeric characters become underscores). CSV files are read with
\code{read_csv_auto}, Parquet with \code{read_parquet}, JSON with \code{read_json_auto},
and \code{.duckdb} databases are attached. Use \code{"-"} to read CSV from standard
input (named \code{stdin}).}

\item{output}{Path to write the result. Format is inferred from the
extension unless \code{output_format} is set. \code{NULL} (the default) prints to
stdout.}

\item{output_format}{Output format override. When \code{"auto"} (the default),
the format is inferred from \code{output}.}

\item{delimiter}{Output column delimiter. Defaults to \code{","}.}

\item{output_delimiter}{Output delimiter. Overrides \code{delimiter} for writing.}

\item{output_header}{If \code{FALSE}, omit the header row when writing
delimited output.}

\item{library}{Character vector of package names to load in the generated
script.}

\item{tidyverse}{If \code{TRUE}, loads the tidyverse and glue packages.}

\item{head}{Integer. Limit the output to this many rows.}

\item{seed}{Integer seed for the random number generator.}

\item{dry_run}{If \code{TRUE}, print the generated script instead of executing
it.}

\item{no_ir}{If \code{TRUE}, execute the script with \code{Rscript} instead of \code{ir}.}

\item{verbose}{If \code{TRUE}, print debugging information to stderr.}
}
\value{
Invisibly returns the exit status of the script (integer), or
\code{NULL} when \code{dry_run = TRUE}.
}
\description{
Generates a self-contained R script that runs a SQL query via DuckDB, then
executes it with \href{https://r-lib.github.io/ir/}{ir}. Input files are
registered as relations that can be referenced in the query.
}
\examples{
# Preview the generated script
rush_sql("SELECT 1 AS x", dry_run = TRUE)

# Query a CSV file
rush_sql("SELECT * FROM data WHERE x > 10",
         file = "data.csv", dry_run = TRUE)

\dontrun{
# Actually execute (requires ir on PATH)
rush_sql("SELECT * FROM a JOIN b USING (id)",
         file = c("a.csv", "b.parquet"))
rush_sql("SELECT * FROM data", file = "data.csv",
         output = "result.parquet")
}

}
\seealso{
\code{\link[=rush_run]{rush_run()}}, \code{\link[=rush_plot]{rush_plot()}}, \code{\link[=rush_convert]{rush_convert()}}
}
