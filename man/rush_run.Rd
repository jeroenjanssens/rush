% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{rush_run}
\alias{rush_run}
\title{Run an R expression}
\usage{
rush_run(
  expr = NULL,
  file = character(),
  output = NULL,
  output_format = "auto",
  input_format = "auto",
  delimiter = ",",
  input_delimiter = NULL,
  output_delimiter = NULL,
  header = TRUE,
  clean_names = TRUE,
  library = NULL,
  tidyverse = FALSE,
  head = NULL,
  sheet = NULL,
  seed = NULL,
  dry_run = FALSE,
  no_ir = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{expr}{A character string of R code to evaluate. The value of the last
expression becomes the result. May also be a language object created with
\code{\link[=quote]{quote()}} or a list of language objects from \code{\link[rlang:defusing-advanced]{rlang::exprs()}}.}

\item{file}{Character vector of input file paths. The reader is chosen by
extension (override with \code{input_format}). Use \code{"-"} to read from standard
input. The first file is available as \code{df}; all files are in a named list
\code{dfs}.}

\item{output}{Path to write the result. Format is inferred from the
extension unless \code{output_format} is set. \code{NULL} (the default) prints to
stdout.}

\item{output_format}{Output format override. When \code{"auto"} (the default),
the format is inferred from \code{output}.}

\item{input_format}{Input format override. When \code{"auto"} (the default), the
format is inferred from the file extension.}

\item{delimiter}{Character used as both input and output column delimiter.
Defaults to \code{","}. Overridden by \code{input_delimiter} or \code{output_delimiter}
when set.}

\item{input_delimiter}{Input delimiter. Overrides \code{delimiter} for reading.}

\item{output_delimiter}{Output delimiter. Overrides \code{delimiter} for writing.}

\item{header}{If \code{FALSE}, the input file is read without column names.}

\item{clean_names}{If \code{TRUE} (the default), column names are cleaned with
\code{\link[janitor:clean_names]{janitor::clean_names()}}.}

\item{library}{Character vector of package names to load in the generated
script.}

\item{tidyverse}{If \code{TRUE}, loads the tidyverse and glue packages.}

\item{head}{Integer. Limit the output to this many rows.}

\item{sheet}{Sheet to read from an Excel file. Either a string (sheet name)
or an integer (sheet index).}

\item{seed}{Integer seed for the random number generator.}

\item{dry_run}{If \code{TRUE}, print the generated script instead of executing
it.}

\item{no_ir}{If \code{TRUE}, execute the script with \code{Rscript} instead of \code{ir}.}

\item{verbose}{If \code{TRUE}, print debugging information to stderr.}
}
\value{
Invisibly returns the exit status of the script (integer), or
\code{NULL} when \code{dry_run = TRUE}.
}
\description{
Generates a self-contained R script from the given expression and input
files, then executes it with \href{https://r-lib.github.io/ir/}{ir}. The value
of the last expression is printed to stdout or written to \code{output}.
}
\examples{
# Preview the generated script for a simple expression
rush_run("1 + 1", dry_run = TRUE)

# Read a file and apply an expression
rush_run("head(df)", file = "data.csv", dry_run = TRUE)

# Use a language object
rush_run(quote(nrow(df)), file = "data.csv", dry_run = TRUE)

\dontrun{
# Actually execute (requires ir on PATH)
rush_run("1 + 1")
rush_run("dplyr::filter(df, x > 10)", file = "data.csv",
         output = "filtered.parquet")
}

}
\seealso{
\code{\link[=rush_sql]{rush_sql()}}, \code{\link[=rush_plot]{rush_plot()}}, \code{\link[=rush_convert]{rush_convert()}}
}
